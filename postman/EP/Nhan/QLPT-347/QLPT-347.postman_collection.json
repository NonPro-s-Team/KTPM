{
  "info": {
    "_postman_id": "b44cca0f-8690-5306-af11-661ee2f1b700",
    "name": "QLPT-347 - Nhan - Product Cart Promotion EP",
    "description": "Bộ 37 testcase Equivalence Partitioning cho Cart quantity, Promotion value/minimum order và Promotion usage limits. Chỉ chạy trên localhost. Mặc định yêu cầu các biến an toàn allow_test_writes và dedicated_test_data_confirmed; các ca tạo order còn yêu cầu allow_order_creation.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Setup",
      "description": "Chạy trước các module để xác minh backend local và lấy token CUSTOMER/ADMIN. Không tính vào 72 testcase BVA.",
      "item": [
        {
          "name": "SETUP-01 | Check Backend",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "exec": [
                  "const baseUrl = String(pm.environment.get('base_url') || '').replace(/[/]$/, '');",
                  "if (!/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(baseUrl)) {",
                  "  throw new Error('Setup chỉ được chạy trên localhost hoặc 127.0.0.1');",
                  "}"
                ],
                "type": "text/javascript"
              }
            },
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('[SETUP-01] HTTP 200', () => pm.response.to.have.status(200));",
                  "pm.test('[SETUP-01] Response JSON', () => pm.response.to.be.json);",
                  "if (pm.response.code === 200) {",
                  "  const body = pm.response.json();",
                  "  pm.test('[SETUP-01] Có danh sách content', () => pm.expect(body.content).to.be.an('array'));",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "header": [],
            "url": "{{base_url}}/api/products?page=0&size=1",
            "description": "Kiểm tra Spring Boot local đã sẵn sàng trước khi chạy BVA."
          },
          "response": []
        },
        {
          "name": "SETUP-02 | Login Customer",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "exec": [
                  "const baseUrl = String(pm.environment.get('base_url') || '').replace(/[/]$/, '');",
                  "if (!/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(baseUrl)) {",
                  "  throw new Error('Setup chỉ được chạy trên localhost hoặc 127.0.0.1');",
                  "}",
                  "",
                  "for (const key of [\"customer_identifier\",\"customer_password\"]) {",
                  "  const value = pm.environment.get(key);",
                  "  if (value === undefined || value === null || String(value).trim() === '') {",
                  "    throw new Error('Thiếu environment: ' + key);",
                  "  }",
                  "}"
                ],
                "type": "text/javascript"
              }
            },
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('[SETUP-02] HTTP 200', () => pm.response.to.have.status(200));",
                  "pm.test('[SETUP-02] Response JSON', () => pm.response.to.be.json);",
                  "if (pm.response.code === 200) {",
                  "  const body = pm.response.json();",
                  "  pm.test('[SETUP-02] Role CUSTOMER', () => pm.expect(body.role).to.equal('CUSTOMER'));",
                  "  pm.test('[SETUP-02] Có accessToken', () => pm.expect(body.accessToken).to.be.a('string').and.not.empty);",
                  "  if (body.role === 'CUSTOMER' && typeof body.accessToken === 'string' && body.accessToken) {",
                  "    pm.environment.set('customer_token', body.accessToken);",
                  "  }",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": "{{base_url}}/api/auth/login",
            "description": "Đăng nhập tài khoản CUSTOMER test riêng và lưu accessToken vào customer_token.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"identifier\": \"{{customer_identifier}}\",\n  \"password\": \"{{customer_password}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "SETUP-03 | Login Admin",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "exec": [
                  "const baseUrl = String(pm.environment.get('base_url') || '').replace(/[/]$/, '');",
                  "if (!/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(baseUrl)) {",
                  "  throw new Error('Setup chỉ được chạy trên localhost hoặc 127.0.0.1');",
                  "}",
                  "",
                  "for (const key of [\"admin_identifier\",\"admin_password\"]) {",
                  "  const value = pm.environment.get(key);",
                  "  if (value === undefined || value === null || String(value).trim() === '') {",
                  "    throw new Error('Thiếu environment: ' + key);",
                  "  }",
                  "}"
                ],
                "type": "text/javascript"
              }
            },
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('[SETUP-03] HTTP 200', () => pm.response.to.have.status(200));",
                  "pm.test('[SETUP-03] Response JSON', () => pm.response.to.be.json);",
                  "if (pm.response.code === 200) {",
                  "  const body = pm.response.json();",
                  "  pm.test('[SETUP-03] Role ADMIN', () => pm.expect(body.role).to.equal('ADMIN'));",
                  "  pm.test('[SETUP-03] Có accessToken', () => pm.expect(body.accessToken).to.be.a('string').and.not.empty);",
                  "  if (body.role === 'ADMIN' && typeof body.accessToken === 'string' && body.accessToken) {",
                  "    pm.environment.set('admin_token', body.accessToken);",
                  "  }",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": "{{base_url}}/api/auth/login",
            "description": "Đăng nhập tài khoản ADMIN test và lưu accessToken vào admin_token.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"identifier\": \"{{admin_identifier}}\",\n  \"password\": \"{{admin_password}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "SETUP-04 | Verify Customer",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "exec": [
                  "const baseUrl = String(pm.environment.get('base_url') || '').replace(/[/]$/, '');",
                  "if (!/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(baseUrl)) {",
                  "  throw new Error('Setup chỉ được chạy trên localhost hoặc 127.0.0.1');",
                  "}",
                  "",
                  "for (const key of [\"customer_token\",\"test_user_id\"]) {",
                  "  const value = pm.environment.get(key);",
                  "  if (value === undefined || value === null || String(value).trim() === '') {",
                  "    throw new Error('Thiếu environment: ' + key);",
                  "  }",
                  "}"
                ],
                "type": "text/javascript"
              }
            },
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('[SETUP-04] HTTP 200', () => pm.response.to.have.status(200));",
                  "if (pm.response.code === 200) {",
                  "  const body = pm.response.json();",
                  "  pm.test('[SETUP-04] Role CUSTOMER', () => pm.expect(body.role).to.equal('CUSTOMER'));",
                  "  pm.test('[SETUP-04] Đúng test_user_id', () => pm.expect(Number(body.id)).to.equal(Number(pm.environment.get('test_user_id'))));",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "method": "GET",
            "header": [],
            "url": "{{base_url}}/api/users/me",
            "description": "Xác minh customer_token thuộc đúng CUSTOMER test_user_id trước khi ghi giỏ/đặt đơn."
          },
          "response": []
        },
        {
          "name": "SETUP-05 | Verify Admin",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "exec": [
                  "const baseUrl = String(pm.environment.get('base_url') || '').replace(/[/]$/, '');",
                  "if (!/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(baseUrl)) {",
                  "  throw new Error('Setup chỉ được chạy trên localhost hoặc 127.0.0.1');",
                  "}",
                  "",
                  "for (const key of [\"admin_token\"]) {",
                  "  const value = pm.environment.get(key);",
                  "  if (value === undefined || value === null || String(value).trim() === '') {",
                  "    throw new Error('Thiếu environment: ' + key);",
                  "  }",
                  "}"
                ],
                "type": "text/javascript"
              }
            },
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('[SETUP-05] HTTP 200', () => pm.response.to.have.status(200));",
                  "if (pm.response.code === 200) {",
                  "  const body = pm.response.json();",
                  "  pm.test('[SETUP-05] Role ADMIN', () => pm.expect(body.role).to.equal('ADMIN'));",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "method": "GET",
            "header": [],
            "url": "{{base_url}}/api/users/me",
            "description": "Xác minh admin_token thuộc tài khoản ADMIN trước khi tạo dữ liệu promotion biên."
          },
          "response": []
        }
      ]
    },
    {
      "name": "QLPT-350 - Cart quantity",
      "description": "Các ca Equivalence Partitioning thuộc QLPT-350 - Cart quantity.",
      "item": [
        {
          "name": "CART-EP-ADD-VALID | Thêm giỏ với quantity hợp lệ",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-350 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** quantity\n\n**Các lớp:** Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\n\n**Lớp được chọn:** Hợp lệ [1,S]\n\n**Giá trị đại diện:** 5\n\n**Kỳ vọng:** HTTP 200; tạo đúng một cart item quantity=5, subtotal đúng và không trừ tồn kho.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-ADD-VALID\",\"kind\":\"cart\",\"name\":\"Thêm giỏ với quantity hợp lệ\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\",\"position\":\"Hợp lệ [1,S]\",\"input\":5,\"http\":200,\"expected\":\"HTTP 200; tạo đúng một cart item quantity=5, subtotal đúng và không trừ tồn kho.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":5,\"key\":\"main\",\"stock\":10,\"baseline\":0,\"expectedQty\":5,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-ADD-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-ADD-VALID\",\"kind\":\"cart\",\"name\":\"Thêm giỏ với quantity hợp lệ\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\",\"position\":\"Hợp lệ [1,S]\",\"input\":5,\"http\":200,\"expected\":\"HTTP 200; tạo đúng một cart item quantity=5, subtotal đúng và không trừ tồn kho.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":5,\"key\":\"main\",\"stock\":10,\"baseline\":0,\"expectedQty\":5,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-ADD-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "CART-EP-ADD-BELOW-MIN | Từ chối quantity nhỏ hơn 1",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-350 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** quantity\n\n**Các lớp:** Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\n\n**Lớp được chọn:** Không hợp lệ: quantity < 1\n\n**Giá trị đại diện:** 0\n\n**Kỳ vọng:** HTTP 400; không tạo item và trạng thái giỏ không đổi.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-ADD-BELOW-MIN\",\"kind\":\"cart\",\"name\":\"Từ chối quantity nhỏ hơn 1\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\",\"position\":\"Không hợp lệ: quantity < 1\",\"input\":0,\"http\":400,\"expected\":\"HTTP 400; không tạo item và trạng thái giỏ không đổi.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":0,\"key\":\"main\",\"stock\":10,\"baseline\":0,\"expectedQty\":0,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-ADD-BELOW-MIN\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-ADD-BELOW-MIN\",\"kind\":\"cart\",\"name\":\"Từ chối quantity nhỏ hơn 1\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\",\"position\":\"Không hợp lệ: quantity < 1\",\"input\":0,\"http\":400,\"expected\":\"HTTP 400; không tạo item và trạng thái giỏ không đổi.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":0,\"key\":\"main\",\"stock\":10,\"baseline\":0,\"expectedQty\":0,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-ADD-BELOW-MIN\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "CART-EP-ADD-OVER-STOCK | Từ chối quantity vượt tồn kho",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-350 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** quantity\n\n**Các lớp:** Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\n\n**Lớp được chọn:** Không hợp lệ: quantity > S\n\n**Giá trị đại diện:** 11\n\n**Kỳ vọng:** HTTP 400; không tạo item khi quantity vượt stockQty=10.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-ADD-OVER-STOCK\",\"kind\":\"cart\",\"name\":\"Từ chối quantity vượt tồn kho\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\",\"position\":\"Không hợp lệ: quantity > S\",\"input\":11,\"http\":400,\"expected\":\"HTTP 400; không tạo item khi quantity vượt stockQty=10.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":11,\"key\":\"main\",\"stock\":10,\"baseline\":0,\"expectedQty\":0,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-ADD-OVER-STOCK\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-ADD-OVER-STOCK\",\"kind\":\"cart\",\"name\":\"Từ chối quantity vượt tồn kho\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\",\"position\":\"Không hợp lệ: quantity > S\",\"input\":11,\"http\":400,\"expected\":\"HTTP 400; không tạo item khi quantity vượt stockQty=10.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":11,\"key\":\"main\",\"stock\":10,\"baseline\":0,\"expectedQty\":0,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-ADD-OVER-STOCK\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "CART-EP-ADD-NULL | Từ chối quantity null",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-350 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** quantity\n\n**Các lớp:** Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\n\n**Lớp được chọn:** Không hợp lệ: null\n\n**Giá trị đại diện:** None\n\n**Kỳ vọng:** HTTP 400 do @NotNull; giỏ không thay đổi.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-ADD-NULL\",\"kind\":\"cart\",\"name\":\"Từ chối quantity null\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\",\"position\":\"Không hợp lệ: null\",\"input\":null,\"http\":400,\"expected\":\"HTTP 400 do @NotNull; giỏ không thay đổi.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":null,\"key\":\"main\",\"stock\":10,\"baseline\":0,\"expectedQty\":0,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"sourceCase\":\"CART-EP-ADD-NULL\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-ADD-NULL\",\"kind\":\"cart\",\"name\":\"Từ chối quantity null\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\",\"position\":\"Không hợp lệ: null\",\"input\":null,\"http\":400,\"expected\":\"HTTP 400 do @NotNull; giỏ không thay đổi.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":null,\"key\":\"main\",\"stock\":10,\"baseline\":0,\"expectedQty\":0,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"sourceCase\":\"CART-EP-ADD-NULL\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "CART-EP-ADD-DECIMAL | Từ chối quantity số thập phân",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-350 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** quantity\n\n**Các lớp:** Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\n\n**Lớp được chọn:** Không hợp lệ: không phải số nguyên\n\n**Giá trị đại diện:** 1.5\n\n**Kỳ vọng:** HTTP 400 do quantity không ánh xạ hợp lệ vào Integer; giỏ không thay đổi.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-ADD-DECIMAL\",\"kind\":\"cart\",\"name\":\"Từ chối quantity số thập phân\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\",\"position\":\"Không hợp lệ: không phải số nguyên\",\"input\":1.5,\"http\":400,\"expected\":\"HTTP 400 do quantity không ánh xạ hợp lệ vào Integer; giỏ không thay đổi.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":1.5,\"key\":\"main\",\"stock\":10,\"baseline\":0,\"expectedQty\":0,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"sourceCase\":\"CART-EP-ADD-DECIMAL\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-ADD-DECIMAL\",\"kind\":\"cart\",\"name\":\"Từ chối quantity số thập phân\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1, >S, null, không phải số nguyên\",\"position\":\"Không hợp lệ: không phải số nguyên\",\"input\":1.5,\"http\":400,\"expected\":\"HTTP 400 do quantity không ánh xạ hợp lệ vào Integer; giỏ không thay đổi.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":1.5,\"key\":\"main\",\"stock\":10,\"baseline\":0,\"expectedQty\":0,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"sourceCase\":\"CART-EP-ADD-DECIMAL\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "CART-EP-UPD-VALID | Cập nhật giỏ với quantity hợp lệ",
          "request": {
            "method": "PUT",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-350 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** quantity\n\n**Các lớp:** Hợp lệ: số nguyên 1..S; Không hợp lệ: <1 hoặc >S\n\n**Lớp được chọn:** Hợp lệ [1,S]\n\n**Giá trị đại diện:** 5\n\n**Kỳ vọng:** HTTP 200; cart item được cập nhật quantity=5, subtotal đúng, kho không đổi.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-UPD-VALID\",\"kind\":\"cart\",\"name\":\"Cập nhật giỏ với quantity hợp lệ\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1 hoặc >S\",\"position\":\"Hợp lệ [1,S]\",\"input\":5,\"http\":200,\"expected\":\"HTTP 200; cart item được cập nhật quantity=5, subtotal đúng, kho không đổi.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"update\",\"q\":5,\"key\":\"main\",\"stock\":10,\"baseline\":5,\"expectedQty\":5,\"method\":\"PUT\",\"api\":\"/api/cart/items/{cartItemId}\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-UPD-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-UPD-VALID\",\"kind\":\"cart\",\"name\":\"Cập nhật giỏ với quantity hợp lệ\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1 hoặc >S\",\"position\":\"Hợp lệ [1,S]\",\"input\":5,\"http\":200,\"expected\":\"HTTP 200; cart item được cập nhật quantity=5, subtotal đúng, kho không đổi.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"update\",\"q\":5,\"key\":\"main\",\"stock\":10,\"baseline\":5,\"expectedQty\":5,\"method\":\"PUT\",\"api\":\"/api/cart/items/{cartItemId}\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-UPD-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "CART-EP-UPD-BELOW-MIN | Từ chối cập nhật quantity nhỏ hơn 1",
          "request": {
            "method": "PUT",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-350 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** quantity\n\n**Các lớp:** Hợp lệ: số nguyên 1..S; Không hợp lệ: <1 hoặc >S\n\n**Lớp được chọn:** Không hợp lệ: quantity < 1\n\n**Giá trị đại diện:** 0\n\n**Kỳ vọng:** HTTP 400; cart item giữ quantity baseline.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-UPD-BELOW-MIN\",\"kind\":\"cart\",\"name\":\"Từ chối cập nhật quantity nhỏ hơn 1\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1 hoặc >S\",\"position\":\"Không hợp lệ: quantity < 1\",\"input\":0,\"http\":400,\"expected\":\"HTTP 400; cart item giữ quantity baseline.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"update\",\"q\":0,\"key\":\"main\",\"stock\":10,\"baseline\":5,\"expectedQty\":5,\"method\":\"PUT\",\"api\":\"/api/cart/items/{cartItemId}\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-UPD-BELOW-MIN\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-UPD-BELOW-MIN\",\"kind\":\"cart\",\"name\":\"Từ chối cập nhật quantity nhỏ hơn 1\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1 hoặc >S\",\"position\":\"Không hợp lệ: quantity < 1\",\"input\":0,\"http\":400,\"expected\":\"HTTP 400; cart item giữ quantity baseline.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"update\",\"q\":0,\"key\":\"main\",\"stock\":10,\"baseline\":5,\"expectedQty\":5,\"method\":\"PUT\",\"api\":\"/api/cart/items/{cartItemId}\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-UPD-BELOW-MIN\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "CART-EP-UPD-OVER-STOCK | Từ chối cập nhật quantity vượt tồn kho",
          "request": {
            "method": "PUT",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-350 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** quantity\n\n**Các lớp:** Hợp lệ: số nguyên 1..S; Không hợp lệ: <1 hoặc >S\n\n**Lớp được chọn:** Không hợp lệ: quantity > S\n\n**Giá trị đại diện:** 11\n\n**Kỳ vọng:** HTTP 400; cart item giữ quantity baseline.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-UPD-OVER-STOCK\",\"kind\":\"cart\",\"name\":\"Từ chối cập nhật quantity vượt tồn kho\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1 hoặc >S\",\"position\":\"Không hợp lệ: quantity > S\",\"input\":11,\"http\":400,\"expected\":\"HTTP 400; cart item giữ quantity baseline.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"update\",\"q\":11,\"key\":\"main\",\"stock\":10,\"baseline\":5,\"expectedQty\":5,\"method\":\"PUT\",\"api\":\"/api/cart/items/{cartItemId}\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-UPD-OVER-STOCK\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-UPD-OVER-STOCK\",\"kind\":\"cart\",\"name\":\"Từ chối cập nhật quantity vượt tồn kho\",\"field\":\"quantity\",\"rule\":\"Hợp lệ: số nguyên 1..S; Không hợp lệ: <1 hoặc >S\",\"position\":\"Không hợp lệ: quantity > S\",\"input\":11,\"http\":400,\"expected\":\"HTTP 400; cart item giữ quantity baseline.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"update\",\"q\":11,\"key\":\"main\",\"stock\":10,\"baseline\":5,\"expectedQty\":5,\"method\":\"PUT\",\"api\":\"/api/cart/items/{cartItemId}\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-UPD-OVER-STOCK\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "CART-EP-SUM-VALID | Cộng dồn quantity còn trong tồn kho",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-350 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** existing.quantity + request.quantity\n\n**Các lớp:** Hợp lệ: tổng <= S; Không hợp lệ: tổng > S\n\n**Lớp được chọn:** Hợp lệ: tổng <= S\n\n**Giá trị đại diện:** 8 + 1 = 9\n\n**Kỳ vọng:** HTTP 200; vẫn một item, quantity sau cộng dồn bằng 9 và kho không đổi.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-SUM-VALID\",\"kind\":\"cart\",\"name\":\"Cộng dồn quantity còn trong tồn kho\",\"field\":\"existing.quantity + request.quantity\",\"rule\":\"Hợp lệ: tổng <= S; Không hợp lệ: tổng > S\",\"position\":\"Hợp lệ: tổng <= S\",\"input\":\"8 + 1 = 9\",\"http\":200,\"expected\":\"HTTP 200; vẫn một item, quantity sau cộng dồn bằng 9 và kho không đổi.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":1,\"key\":\"main\",\"stock\":10,\"baseline\":8,\"expectedQty\":9,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-SUM-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-SUM-VALID\",\"kind\":\"cart\",\"name\":\"Cộng dồn quantity còn trong tồn kho\",\"field\":\"existing.quantity + request.quantity\",\"rule\":\"Hợp lệ: tổng <= S; Không hợp lệ: tổng > S\",\"position\":\"Hợp lệ: tổng <= S\",\"input\":\"8 + 1 = 9\",\"http\":200,\"expected\":\"HTTP 200; vẫn một item, quantity sau cộng dồn bằng 9 và kho không đổi.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":1,\"key\":\"main\",\"stock\":10,\"baseline\":8,\"expectedQty\":9,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-SUM-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "CART-EP-SUM-OVER-STOCK | Từ chối cộng dồn vượt tồn kho",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-350 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** existing.quantity + request.quantity\n\n**Các lớp:** Hợp lệ: tổng <= S; Không hợp lệ: tổng > S\n\n**Lớp được chọn:** Không hợp lệ: tổng > S\n\n**Giá trị đại diện:** 8 + 3 = 11\n\n**Kỳ vọng:** HTTP 400; quantity vẫn bằng baseline 8.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-SUM-OVER-STOCK\",\"kind\":\"cart\",\"name\":\"Từ chối cộng dồn vượt tồn kho\",\"field\":\"existing.quantity + request.quantity\",\"rule\":\"Hợp lệ: tổng <= S; Không hợp lệ: tổng > S\",\"position\":\"Không hợp lệ: tổng > S\",\"input\":\"8 + 3 = 11\",\"http\":400,\"expected\":\"HTTP 400; quantity vẫn bằng baseline 8.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":3,\"key\":\"main\",\"stock\":10,\"baseline\":8,\"expectedQty\":8,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-SUM-OVER-STOCK\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Cart\",\"id\":\"CART-EP-SUM-OVER-STOCK\",\"kind\":\"cart\",\"name\":\"Từ chối cộng dồn vượt tồn kho\",\"field\":\"existing.quantity + request.quantity\",\"rule\":\"Hợp lệ: tổng <= S; Không hợp lệ: tổng > S\",\"position\":\"Không hợp lệ: tổng > S\",\"input\":\"8 + 3 = 11\",\"http\":400,\"expected\":\"HTTP 400; quantity vẫn bằng baseline 8.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"AddToCartRequest/UpdateCartItemRequest: @NotNull, @Min(1); CartServiceImpl.addItem/updateItem: quantity và tổng quantity <= stockQty.\",\"issue\":\"QLPT-350\",\"mode\":\"add\",\"q\":3,\"key\":\"main\",\"stock\":10,\"baseline\":8,\"expectedQty\":8,\"method\":\"POST\",\"api\":\"/api/cart/items\",\"preconditions\":\"CUSTOMER test riêng, giỏ trống. Bật allow_test_writes. Script đọc lại stock, tạo baseline nếu cần, gửi request, GET đối chiếu và chỉ xóa item của ca.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"CART-EP-SUM-OVER-STOCK\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ]
    },
    {
      "name": "QLPT-351 - Promotion value and minimum",
      "description": "Các ca Equivalence Partitioning thuộc QLPT-351 - Promotion value and minimum.",
      "item": [
        {
          "name": "PROMO-EP-PCT-VALID | Tạo và áp dụng phần trăm hợp lệ",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** value (PERCENT)\n\n**Các lớp:** Hợp lệ: 0.01..100; Không hợp lệ: null, <0.01, >100\n\n**Lớp được chọn:** Hợp lệ [0.01,100]\n\n**Giá trị đại diện:** 50\n\n**Kỳ vọng:** Admin tạo HTTP 201; Customer apply HTTP 200; discount=floor(subtotal×50/100).",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PCT-VALID\",\"kind\":\"admin\",\"name\":\"Tạo và áp dụng phần trăm hợp lệ\",\"field\":\"value (PERCENT)\",\"rule\":\"Hợp lệ: 0.01..100; Không hợp lệ: null, <0.01, >100\",\"position\":\"Hợp lệ [0.01,100]\",\"input\":50,\"http\":201,\"expected\":\"Admin tạo HTTP 201; Customer apply HTTP 200; discount=floor(subtotal×50/100).\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"PERCENT\",\"value\":50},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PCT-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PCT-VALID\",\"kind\":\"admin\",\"name\":\"Tạo và áp dụng phần trăm hợp lệ\",\"field\":\"value (PERCENT)\",\"rule\":\"Hợp lệ: 0.01..100; Không hợp lệ: null, <0.01, >100\",\"position\":\"Hợp lệ [0.01,100]\",\"input\":50,\"http\":201,\"expected\":\"Admin tạo HTTP 201; Customer apply HTTP 200; discount=floor(subtotal×50/100).\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"PERCENT\",\"value\":50},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PCT-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-PCT-BELOW-MIN | Từ chối phần trăm nhỏ hơn mức tối thiểu",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** value (PERCENT)\n\n**Các lớp:** Hợp lệ: 0.01..100; Không hợp lệ: null, <0.01, >100\n\n**Lớp được chọn:** Không hợp lệ: value < 0.01\n\n**Giá trị đại diện:** 0\n\n**Kỳ vọng:** Admin trả HTTP 400 và không tạo promotion.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PCT-BELOW-MIN\",\"kind\":\"admin\",\"name\":\"Từ chối phần trăm nhỏ hơn mức tối thiểu\",\"field\":\"value (PERCENT)\",\"rule\":\"Hợp lệ: 0.01..100; Không hợp lệ: null, <0.01, >100\",\"position\":\"Không hợp lệ: value < 0.01\",\"input\":0,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"PERCENT\",\"value\":0},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PCT-BELOW-MIN\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PCT-BELOW-MIN\",\"kind\":\"admin\",\"name\":\"Từ chối phần trăm nhỏ hơn mức tối thiểu\",\"field\":\"value (PERCENT)\",\"rule\":\"Hợp lệ: 0.01..100; Không hợp lệ: null, <0.01, >100\",\"position\":\"Không hợp lệ: value < 0.01\",\"input\":0,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"PERCENT\",\"value\":0},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PCT-BELOW-MIN\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-PCT-OVER-MAX | Từ chối phần trăm lớn hơn 100",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** value (PERCENT)\n\n**Các lớp:** Hợp lệ: 0.01..100; Không hợp lệ: null, <0.01, >100\n\n**Lớp được chọn:** Không hợp lệ: value > 100\n\n**Giá trị đại diện:** 100.01\n\n**Kỳ vọng:** Admin trả HTTP 400 và không tạo promotion.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PCT-OVER-MAX\",\"kind\":\"admin\",\"name\":\"Từ chối phần trăm lớn hơn 100\",\"field\":\"value (PERCENT)\",\"rule\":\"Hợp lệ: 0.01..100; Không hợp lệ: null, <0.01, >100\",\"position\":\"Không hợp lệ: value > 100\",\"input\":100.01,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"PERCENT\",\"value\":100.01},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PCT-OVER-MAX\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PCT-OVER-MAX\",\"kind\":\"admin\",\"name\":\"Từ chối phần trăm lớn hơn 100\",\"field\":\"value (PERCENT)\",\"rule\":\"Hợp lệ: 0.01..100; Không hợp lệ: null, <0.01, >100\",\"position\":\"Không hợp lệ: value > 100\",\"input\":100.01,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"PERCENT\",\"value\":100.01},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PCT-OVER-MAX\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-PCT-NULL | Từ chối giá trị promotion null",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** value\n\n**Các lớp:** Hợp lệ: value có số dương theo type; Không hợp lệ: null\n\n**Lớp được chọn:** Không hợp lệ: null\n\n**Giá trị đại diện:** None\n\n**Kỳ vọng:** Admin trả HTTP 400 do value bắt buộc.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PCT-NULL\",\"kind\":\"admin\",\"name\":\"Từ chối giá trị promotion null\",\"field\":\"value\",\"rule\":\"Hợp lệ: value có số dương theo type; Không hợp lệ: null\",\"position\":\"Không hợp lệ: null\",\"input\":null,\"http\":400,\"expected\":\"Admin trả HTTP 400 do value bắt buộc.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"PERCENT\",\"value\":null},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PCT-NULL\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PCT-NULL\",\"kind\":\"admin\",\"name\":\"Từ chối giá trị promotion null\",\"field\":\"value\",\"rule\":\"Hợp lệ: value có số dương theo type; Không hợp lệ: null\",\"position\":\"Không hợp lệ: null\",\"input\":null,\"http\":400,\"expected\":\"Admin trả HTTP 400 do value bắt buộc.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"PERCENT\",\"value\":null},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PCT-NULL\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-FIXED-VALID | Áp dụng giảm cố định hợp lệ",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** value (FIXED)\n\n**Các lớp:** Hợp lệ: value >=0.01; Không hợp lệ: null hoặc <0.01; khi value>subtotal thì discount bị chặn bằng subtotal\n\n**Lớp được chọn:** Hợp lệ: 0.01 <= value <= subtotal\n\n**Giá trị đại diện:** subtotal - 0.01\n\n**Kỳ vọng:** Admin tạo HTTP 201; apply HTTP 200; discount=value và tổng còn 0.01.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-FIXED-VALID\",\"kind\":\"admin\",\"name\":\"Áp dụng giảm cố định hợp lệ\",\"field\":\"value (FIXED)\",\"rule\":\"Hợp lệ: value >=0.01; Không hợp lệ: null hoặc <0.01; khi value>subtotal thì discount bị chặn bằng subtotal\",\"position\":\"Hợp lệ: 0.01 <= value <= subtotal\",\"input\":\"subtotal - 0.01\",\"http\":201,\"expected\":\"Admin tạo HTTP 201; apply HTTP 200; discount=value và tổng còn 0.01.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"FIXED\"},\"valueDelta\":-0.01,\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"sourceCase\":\"PROMO-EP-FIXED-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-FIXED-VALID\",\"kind\":\"admin\",\"name\":\"Áp dụng giảm cố định hợp lệ\",\"field\":\"value (FIXED)\",\"rule\":\"Hợp lệ: value >=0.01; Không hợp lệ: null hoặc <0.01; khi value>subtotal thì discount bị chặn bằng subtotal\",\"position\":\"Hợp lệ: 0.01 <= value <= subtotal\",\"input\":\"subtotal - 0.01\",\"http\":201,\"expected\":\"Admin tạo HTTP 201; apply HTTP 200; discount=value và tổng còn 0.01.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"FIXED\"},\"valueDelta\":-0.01,\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"sourceCase\":\"PROMO-EP-FIXED-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-FIXED-CAPPED | Chặn giảm cố định lớn hơn subtotal",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** value (FIXED)\n\n**Các lớp:** Hợp lệ: value >=0.01; Không hợp lệ: null hoặc <0.01; khi value>subtotal thì discount bị chặn bằng subtotal\n\n**Lớp được chọn:** Hợp lệ đặc biệt: value > subtotal\n\n**Giá trị đại diện:** subtotal + 0.01\n\n**Kỳ vọng:** Admin tạo HTTP 201; apply HTTP 200; discount=subtotal và totalAfterDiscount=0.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-FIXED-CAPPED\",\"kind\":\"admin\",\"name\":\"Chặn giảm cố định lớn hơn subtotal\",\"field\":\"value (FIXED)\",\"rule\":\"Hợp lệ: value >=0.01; Không hợp lệ: null hoặc <0.01; khi value>subtotal thì discount bị chặn bằng subtotal\",\"position\":\"Hợp lệ đặc biệt: value > subtotal\",\"input\":\"subtotal + 0.01\",\"http\":201,\"expected\":\"Admin tạo HTTP 201; apply HTTP 200; discount=subtotal và totalAfterDiscount=0.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"FIXED\"},\"valueDelta\":0.01,\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"sourceCase\":\"PROMO-EP-FIXED-CAPPED\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-FIXED-CAPPED\",\"kind\":\"admin\",\"name\":\"Chặn giảm cố định lớn hơn subtotal\",\"field\":\"value (FIXED)\",\"rule\":\"Hợp lệ: value >=0.01; Không hợp lệ: null hoặc <0.01; khi value>subtotal thì discount bị chặn bằng subtotal\",\"position\":\"Hợp lệ đặc biệt: value > subtotal\",\"input\":\"subtotal + 0.01\",\"http\":201,\"expected\":\"Admin tạo HTTP 201; apply HTTP 200; discount=subtotal và totalAfterDiscount=0.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"FIXED\"},\"valueDelta\":0.01,\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"sourceCase\":\"PROMO-EP-FIXED-CAPPED\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-FIXED-INVALID-ZERO | Từ chối giảm cố định bằng 0",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** value (FIXED)\n\n**Các lớp:** Hợp lệ: value >=0.01; Không hợp lệ: null hoặc <0.01\n\n**Lớp được chọn:** Không hợp lệ: value < 0.01\n\n**Giá trị đại diện:** 0\n\n**Kỳ vọng:** Admin trả HTTP 400 và không tạo promotion.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-FIXED-INVALID-ZERO\",\"kind\":\"admin\",\"name\":\"Từ chối giảm cố định bằng 0\",\"field\":\"value (FIXED)\",\"rule\":\"Hợp lệ: value >=0.01; Không hợp lệ: null hoặc <0.01\",\"position\":\"Không hợp lệ: value < 0.01\",\"input\":0,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"FIXED\",\"value\":0},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-FIXED-INVALID-ZERO\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-FIXED-INVALID-ZERO\",\"kind\":\"admin\",\"name\":\"Từ chối giảm cố định bằng 0\",\"field\":\"value (FIXED)\",\"rule\":\"Hợp lệ: value >=0.01; Không hợp lệ: null hoặc <0.01\",\"position\":\"Không hợp lệ: value < 0.01\",\"input\":0,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"type\":\"FIXED\",\"value\":0},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-FIXED-INVALID-ZERO\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-MINCFG-VALID-ZERO | Tạo promotion với minOrderValue bằng 0",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** minOrderValue\n\n**Các lớp:** Hợp lệ: null hoặc >=0; Không hợp lệ: <0\n\n**Lớp được chọn:** Hợp lệ: 0\n\n**Giá trị đại diện:** 0\n\n**Kỳ vọng:** Admin tạo HTTP 201; Customer có thể áp dụng khi các điều kiện khác hợp lệ.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MINCFG-VALID-ZERO\",\"kind\":\"admin\",\"name\":\"Tạo promotion với minOrderValue bằng 0\",\"field\":\"minOrderValue\",\"rule\":\"Hợp lệ: null hoặc >=0; Không hợp lệ: <0\",\"position\":\"Hợp lệ: 0\",\"input\":0,\"http\":201,\"expected\":\"Admin tạo HTTP 201; Customer có thể áp dụng khi các điều kiện khác hợp lệ.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"minOrderValue\":0},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MINCFG-VALID-ZERO\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MINCFG-VALID-ZERO\",\"kind\":\"admin\",\"name\":\"Tạo promotion với minOrderValue bằng 0\",\"field\":\"minOrderValue\",\"rule\":\"Hợp lệ: null hoặc >=0; Không hợp lệ: <0\",\"position\":\"Hợp lệ: 0\",\"input\":0,\"http\":201,\"expected\":\"Admin tạo HTTP 201; Customer có thể áp dụng khi các điều kiện khác hợp lệ.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"minOrderValue\":0},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MINCFG-VALID-ZERO\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-MINCFG-VALID-POSITIVE | Tạo promotion với minOrderValue dương",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** minOrderValue\n\n**Các lớp:** Hợp lệ: null hoặc >=0; Không hợp lệ: <0\n\n**Lớp được chọn:** Hợp lệ: >0\n\n**Giá trị đại diện:** 0.01\n\n**Kỳ vọng:** Admin tạo HTTP 201 và lưu đúng minOrderValue.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MINCFG-VALID-POSITIVE\",\"kind\":\"admin\",\"name\":\"Tạo promotion với minOrderValue dương\",\"field\":\"minOrderValue\",\"rule\":\"Hợp lệ: null hoặc >=0; Không hợp lệ: <0\",\"position\":\"Hợp lệ: >0\",\"input\":0.01,\"http\":201,\"expected\":\"Admin tạo HTTP 201 và lưu đúng minOrderValue.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"minOrderValue\":0.01},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MINCFG-VALID-POSITIVE\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MINCFG-VALID-POSITIVE\",\"kind\":\"admin\",\"name\":\"Tạo promotion với minOrderValue dương\",\"field\":\"minOrderValue\",\"rule\":\"Hợp lệ: null hoặc >=0; Không hợp lệ: <0\",\"position\":\"Hợp lệ: >0\",\"input\":0.01,\"http\":201,\"expected\":\"Admin tạo HTTP 201 và lưu đúng minOrderValue.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"minOrderValue\":0.01},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MINCFG-VALID-POSITIVE\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-MINCFG-DEFAULT-NULL | Dùng mặc định khi minOrderValue null",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** minOrderValue\n\n**Các lớp:** Hợp lệ: null hoặc >=0; Không hợp lệ: <0\n\n**Lớp được chọn:** Hợp lệ đặc biệt: null được chuẩn hóa thành 0\n\n**Giá trị đại diện:** null\n\n**Kỳ vọng:** Admin tạo HTTP 201; response lưu minOrderValue=0; Customer apply HTTP 200 khi các điều kiện khác hợp lệ.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MINCFG-DEFAULT-NULL\",\"kind\":\"admin\",\"name\":\"Dùng mặc định khi minOrderValue null\",\"field\":\"minOrderValue\",\"rule\":\"Hợp lệ: null hoặc >=0; Không hợp lệ: <0\",\"position\":\"Hợp lệ đặc biệt: null được chuẩn hóa thành 0\",\"input\":null,\"http\":201,\"expected\":\"Admin tạo HTTP 201; response lưu minOrderValue=0; Customer apply HTTP 200 khi các điều kiện khác hợp lệ.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: minOrderValue null được chuẩn hóa thành 0; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"minOrderValue\":null},\"apply\":true,\"expectedStored\":{\"minOrderValue\":0},\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; minOrderValue null phải được backend chuẩn hóa thành 0.\",\"sourceCase\":\"PROMO-EP-MINCFG-DEFAULT-NULL\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MINCFG-DEFAULT-NULL\",\"kind\":\"admin\",\"name\":\"Dùng mặc định khi minOrderValue null\",\"field\":\"minOrderValue\",\"rule\":\"Hợp lệ: null hoặc >=0; Không hợp lệ: <0\",\"position\":\"Hợp lệ đặc biệt: null được chuẩn hóa thành 0\",\"input\":null,\"http\":201,\"expected\":\"Admin tạo HTTP 201; response lưu minOrderValue=0; Customer apply HTTP 200 khi các điều kiện khác hợp lệ.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: minOrderValue null được chuẩn hóa thành 0; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"minOrderValue\":null},\"apply\":true,\"expectedStored\":{\"minOrderValue\":0},\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; minOrderValue null phải được backend chuẩn hóa thành 0.\",\"sourceCase\":\"PROMO-EP-MINCFG-DEFAULT-NULL\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-MINCFG-NEGATIVE | Từ chối minOrderValue âm",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** minOrderValue\n\n**Các lớp:** Hợp lệ: null hoặc >=0; Không hợp lệ: <0\n\n**Lớp được chọn:** Không hợp lệ: <0\n\n**Giá trị đại diện:** -0.01\n\n**Kỳ vọng:** Admin trả HTTP 400 và không tạo promotion.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MINCFG-NEGATIVE\",\"kind\":\"admin\",\"name\":\"Từ chối minOrderValue âm\",\"field\":\"minOrderValue\",\"rule\":\"Hợp lệ: null hoặc >=0; Không hợp lệ: <0\",\"position\":\"Không hợp lệ: <0\",\"input\":-0.01,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"minOrderValue\":-0.01},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MINCFG-NEGATIVE\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MINCFG-NEGATIVE\",\"kind\":\"admin\",\"name\":\"Từ chối minOrderValue âm\",\"field\":\"minOrderValue\",\"rule\":\"Hợp lệ: null hoặc >=0; Không hợp lệ: <0\",\"position\":\"Không hợp lệ: <0\",\"input\":-0.01,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"patch\":{\"minOrderValue\":-0.01},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MINCFG-NEGATIVE\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-MIN-NOT-MET | Từ chối áp mã khi chưa đạt đơn tối thiểu",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** subtotal so với minOrderValue\n\n**Các lớp:** Hợp lệ: subtotal >= minOrderValue; Không hợp lệ: subtotal < minOrderValue\n\n**Lớp được chọn:** Không hợp lệ: subtotal < minOrderValue\n\n**Giá trị đại diện:** subtotal=S; minOrderValue=S+0.01\n\n**Kỳ vọng:** Apply trả HTTP 400; không tăng lượt sử dụng.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MIN-NOT-MET\",\"kind\":\"preview\",\"name\":\"Từ chối áp mã khi chưa đạt đơn tối thiểu\",\"field\":\"subtotal so với minOrderValue\",\"rule\":\"Hợp lệ: subtotal >= minOrderValue; Không hợp lệ: subtotal < minOrderValue\",\"position\":\"Không hợp lệ: subtotal < minOrderValue\",\"input\":\"subtotal=S; minOrderValue=S+0.01\",\"http\":400,\"expected\":\"Apply trả HTTP 400; không tăng lượt sử dụng.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"minDelta\":0.01,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MIN-NOT-MET\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MIN-NOT-MET\",\"kind\":\"preview\",\"name\":\"Từ chối áp mã khi chưa đạt đơn tối thiểu\",\"field\":\"subtotal so với minOrderValue\",\"rule\":\"Hợp lệ: subtotal >= minOrderValue; Không hợp lệ: subtotal < minOrderValue\",\"position\":\"Không hợp lệ: subtotal < minOrderValue\",\"input\":\"subtotal=S; minOrderValue=S+0.01\",\"http\":400,\"expected\":\"Apply trả HTTP 400; không tăng lượt sử dụng.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"minDelta\":0.01,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MIN-NOT-MET\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-MIN-MET | Áp mã khi đạt đơn tối thiểu",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-351 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** subtotal so với minOrderValue\n\n**Các lớp:** Hợp lệ: subtotal >= minOrderValue; Không hợp lệ: subtotal < minOrderValue\n\n**Lớp được chọn:** Hợp lệ: subtotal >= minOrderValue\n\n**Giá trị đại diện:** subtotal=S; minOrderValue=S-0.01\n\n**Kỳ vọng:** Apply trả HTTP 200; discount và total tính đúng; preview không tăng lượt.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MIN-MET\",\"kind\":\"preview\",\"name\":\"Áp mã khi đạt đơn tối thiểu\",\"field\":\"subtotal so với minOrderValue\",\"rule\":\"Hợp lệ: subtotal >= minOrderValue; Không hợp lệ: subtotal < minOrderValue\",\"position\":\"Hợp lệ: subtotal >= minOrderValue\",\"input\":\"subtotal=S; minOrderValue=S-0.01\",\"http\":200,\"expected\":\"Apply trả HTTP 200; discount và total tính đúng; preview không tăng lượt.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"minDelta\":-0.01,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MIN-MET\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MIN-MET\",\"kind\":\"preview\",\"name\":\"Áp mã khi đạt đơn tối thiểu\",\"field\":\"subtotal so với minOrderValue\",\"rule\":\"Hợp lệ: subtotal >= minOrderValue; Không hợp lệ: subtotal < minOrderValue\",\"position\":\"Hợp lệ: subtotal >= minOrderValue\",\"input\":\"subtotal=S; minOrderValue=S-0.01\",\"http\":200,\"expected\":\"Apply trả HTTP 200; discount và total tính đúng; preview không tăng lượt.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-351\",\"minDelta\":-0.01,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MIN-MET\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ]
    },
    {
      "name": "QLPT-352 - Promotion usage limits",
      "description": "Các ca Equivalence Partitioning thuộc QLPT-352 - Promotion usage limits.",
      "item": [
        {
          "name": "PROMO-EP-MAXUSES-ZERO | Từ chối maxUses bằng 0",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** maxUses\n\n**Các lớp:** Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\n\n**Lớp được chọn:** Không hợp lệ: <=0\n\n**Giá trị đại diện:** 0\n\n**Kỳ vọng:** Admin trả HTTP 400 và không tạo promotion.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MAXUSES-ZERO\",\"kind\":\"admin\",\"name\":\"Từ chối maxUses bằng 0\",\"field\":\"maxUses\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Không hợp lệ: <=0\",\"input\":0,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUses\":0},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MAXUSES-ZERO\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MAXUSES-ZERO\",\"kind\":\"admin\",\"name\":\"Từ chối maxUses bằng 0\",\"field\":\"maxUses\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Không hợp lệ: <=0\",\"input\":0,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUses\":0},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MAXUSES-ZERO\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-MAXUSES-POSITIVE | Chấp nhận maxUses dương",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** maxUses\n\n**Các lớp:** Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\n\n**Lớp được chọn:** Hợp lệ: số nguyên >=1\n\n**Giá trị đại diện:** 1\n\n**Kỳ vọng:** Admin tạo HTTP 201; lưu maxUses=1; mã mới còn lượt.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MAXUSES-POSITIVE\",\"kind\":\"admin\",\"name\":\"Chấp nhận maxUses dương\",\"field\":\"maxUses\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Hợp lệ: số nguyên >=1\",\"input\":1,\"http\":201,\"expected\":\"Admin tạo HTTP 201; lưu maxUses=1; mã mới còn lượt.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUses\":1},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MAXUSES-POSITIVE\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MAXUSES-POSITIVE\",\"kind\":\"admin\",\"name\":\"Chấp nhận maxUses dương\",\"field\":\"maxUses\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Hợp lệ: số nguyên >=1\",\"input\":1,\"http\":201,\"expected\":\"Admin tạo HTTP 201; lưu maxUses=1; mã mới còn lượt.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUses\":1},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MAXUSES-POSITIVE\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-MAXUSES-UNLIMITED | Chấp nhận maxUses null",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** maxUses\n\n**Các lớp:** Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\n\n**Lớp được chọn:** Hợp lệ đặc biệt: null = không giới hạn\n\n**Giá trị đại diện:** None\n\n**Kỳ vọng:** Admin tạo HTTP 201; maxUses=null và mã dùng được.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MAXUSES-UNLIMITED\",\"kind\":\"admin\",\"name\":\"Chấp nhận maxUses null\",\"field\":\"maxUses\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Hợp lệ đặc biệt: null = không giới hạn\",\"input\":null,\"http\":201,\"expected\":\"Admin tạo HTTP 201; maxUses=null và mã dùng được.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUses\":null},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MAXUSES-UNLIMITED\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-MAXUSES-UNLIMITED\",\"kind\":\"admin\",\"name\":\"Chấp nhận maxUses null\",\"field\":\"maxUses\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Hợp lệ đặc biệt: null = không giới hạn\",\"input\":null,\"http\":201,\"expected\":\"Admin tạo HTTP 201; maxUses=null và mã dùng được.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUses\":null},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-MAXUSES-UNLIMITED\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-PERUSER-ZERO | Từ chối maxUsesPerUser bằng 0",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** maxUsesPerUser\n\n**Các lớp:** Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\n\n**Lớp được chọn:** Không hợp lệ: <=0\n\n**Giá trị đại diện:** 0\n\n**Kỳ vọng:** Admin trả HTTP 400 và không tạo promotion.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PERUSER-ZERO\",\"kind\":\"admin\",\"name\":\"Từ chối maxUsesPerUser bằng 0\",\"field\":\"maxUsesPerUser\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Không hợp lệ: <=0\",\"input\":0,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUsesPerUser\":0},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PERUSER-ZERO\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PERUSER-ZERO\",\"kind\":\"admin\",\"name\":\"Từ chối maxUsesPerUser bằng 0\",\"field\":\"maxUsesPerUser\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Không hợp lệ: <=0\",\"input\":0,\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUsesPerUser\":0},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PERUSER-ZERO\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-PERUSER-POSITIVE | Chấp nhận maxUsesPerUser dương",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** maxUsesPerUser\n\n**Các lớp:** Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\n\n**Lớp được chọn:** Hợp lệ: số nguyên >=1\n\n**Giá trị đại diện:** 1\n\n**Kỳ vọng:** Admin tạo HTTP 201; lưu maxUsesPerUser=1; user chưa dùng nên mã hợp lệ.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PERUSER-POSITIVE\",\"kind\":\"admin\",\"name\":\"Chấp nhận maxUsesPerUser dương\",\"field\":\"maxUsesPerUser\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Hợp lệ: số nguyên >=1\",\"input\":1,\"http\":201,\"expected\":\"Admin tạo HTTP 201; lưu maxUsesPerUser=1; user chưa dùng nên mã hợp lệ.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUsesPerUser\":1},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PERUSER-POSITIVE\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PERUSER-POSITIVE\",\"kind\":\"admin\",\"name\":\"Chấp nhận maxUsesPerUser dương\",\"field\":\"maxUsesPerUser\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Hợp lệ: số nguyên >=1\",\"input\":1,\"http\":201,\"expected\":\"Admin tạo HTTP 201; lưu maxUsesPerUser=1; user chưa dùng nên mã hợp lệ.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUsesPerUser\":1},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PERUSER-POSITIVE\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-PERUSER-UNLIMITED | Chấp nhận maxUsesPerUser null",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** maxUsesPerUser\n\n**Các lớp:** Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\n\n**Lớp được chọn:** Hợp lệ đặc biệt: null = không giới hạn\n\n**Giá trị đại diện:** None\n\n**Kỳ vọng:** Admin tạo HTTP 201; maxUsesPerUser=null và mã dùng được.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PERUSER-UNLIMITED\",\"kind\":\"admin\",\"name\":\"Chấp nhận maxUsesPerUser null\",\"field\":\"maxUsesPerUser\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Hợp lệ đặc biệt: null = không giới hạn\",\"input\":null,\"http\":201,\"expected\":\"Admin tạo HTTP 201; maxUsesPerUser=null và mã dùng được.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUsesPerUser\":null},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PERUSER-UNLIMITED\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-PERUSER-UNLIMITED\",\"kind\":\"admin\",\"name\":\"Chấp nhận maxUsesPerUser null\",\"field\":\"maxUsesPerUser\",\"rule\":\"Hợp lệ: null hoặc số nguyên >=1; Không hợp lệ: <=0\",\"position\":\"Hợp lệ đặc biệt: null = không giới hạn\",\"input\":null,\"http\":201,\"expected\":\"Admin tạo HTTP 201; maxUsesPerUser=null và mã dùng được.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUsesPerUser\":null},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-PERUSER-UNLIMITED\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-RELATION-VALID | Chấp nhận giới hạn mỗi user không vượt tổng",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** maxUsesPerUser so với maxUses\n\n**Các lớp:** Hợp lệ: một field null hoặc maxUsesPerUser<=maxUses; Không hợp lệ: maxUsesPerUser>maxUses\n\n**Lớp được chọn:** Hợp lệ: maxUsesPerUser <= maxUses\n\n**Giá trị đại diện:** maxUses=2; maxUsesPerUser=1\n\n**Kỳ vọng:** Admin tạo HTTP 201 và lưu đúng hai giới hạn.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-RELATION-VALID\",\"kind\":\"admin\",\"name\":\"Chấp nhận giới hạn mỗi user không vượt tổng\",\"field\":\"maxUsesPerUser so với maxUses\",\"rule\":\"Hợp lệ: một field null hoặc maxUsesPerUser<=maxUses; Không hợp lệ: maxUsesPerUser>maxUses\",\"position\":\"Hợp lệ: maxUsesPerUser <= maxUses\",\"input\":\"maxUses=2; maxUsesPerUser=1\",\"http\":201,\"expected\":\"Admin tạo HTTP 201 và lưu đúng hai giới hạn.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUses\":2,\"maxUsesPerUser\":1},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-RELATION-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-RELATION-VALID\",\"kind\":\"admin\",\"name\":\"Chấp nhận giới hạn mỗi user không vượt tổng\",\"field\":\"maxUsesPerUser so với maxUses\",\"rule\":\"Hợp lệ: một field null hoặc maxUsesPerUser<=maxUses; Không hợp lệ: maxUsesPerUser>maxUses\",\"position\":\"Hợp lệ: maxUsesPerUser <= maxUses\",\"input\":\"maxUses=2; maxUsesPerUser=1\",\"http\":201,\"expected\":\"Admin tạo HTTP 201 và lưu đúng hai giới hạn.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUses\":2,\"maxUsesPerUser\":1},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-RELATION-VALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-RELATION-INVALID | Từ chối giới hạn mỗi user vượt tổng",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** maxUsesPerUser so với maxUses\n\n**Các lớp:** Hợp lệ: một field null hoặc maxUsesPerUser<=maxUses; Không hợp lệ: maxUsesPerUser>maxUses\n\n**Lớp được chọn:** Không hợp lệ: maxUsesPerUser > maxUses\n\n**Giá trị đại diện:** maxUses=2; maxUsesPerUser=3\n\n**Kỳ vọng:** Admin trả HTTP 400 và không tạo promotion.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{admin_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-RELATION-INVALID\",\"kind\":\"admin\",\"name\":\"Từ chối giới hạn mỗi user vượt tổng\",\"field\":\"maxUsesPerUser so với maxUses\",\"rule\":\"Hợp lệ: một field null hoặc maxUsesPerUser<=maxUses; Không hợp lệ: maxUsesPerUser>maxUses\",\"position\":\"Không hợp lệ: maxUsesPerUser > maxUses\",\"input\":\"maxUses=2; maxUsesPerUser=3\",\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUses\":2,\"maxUsesPerUser\":3},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-RELATION-INVALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-RELATION-INVALID\",\"kind\":\"admin\",\"name\":\"Từ chối giới hạn mỗi user vượt tổng\",\"field\":\"maxUsesPerUser so với maxUses\",\"rule\":\"Hợp lệ: một field null hoặc maxUsesPerUser<=maxUses; Không hợp lệ: maxUsesPerUser>maxUses\",\"position\":\"Không hợp lệ: maxUsesPerUser > maxUses\",\"input\":\"maxUses=2; maxUsesPerUser=3\",\"http\":400,\"expected\":\"Admin trả HTTP 400 và không tạo promotion.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"patch\":{\"maxUses\":2,\"maxUsesPerUser\":3},\"apply\":true,\"method\":\"POST\",\"api\":\"/api/admin/promotions → /api/orders/apply-promo\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-RELATION-INVALID\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-GLOBAL-BELOW-LIMIT | Áp mã khi tổng lượt chưa hết",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** usedCount so với maxUses\n\n**Các lớp:** Hợp lệ: usedCount < maxUses hoặc maxUses=null; Không hợp lệ: usedCount >= maxUses\n\n**Lớp được chọn:** Hợp lệ: usedCount < maxUses\n\n**Giá trị đại diện:** usedCount=2; maxUses=3\n\n**Kỳ vọng:** Apply trả HTTP 200; preview không tăng usedCount.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-GLOBAL-BELOW-LIMIT\",\"kind\":\"usage\",\"name\":\"Áp mã khi tổng lượt chưa hết\",\"field\":\"usedCount so với maxUses\",\"rule\":\"Hợp lệ: usedCount < maxUses hoặc maxUses=null; Không hợp lệ: usedCount >= maxUses\",\"position\":\"Hợp lệ: usedCount < maxUses\",\"input\":\"usedCount=2; maxUses=3\",\"http\":200,\"expected\":\"Apply trả HTTP 200; preview không tăng usedCount.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"global\",\"used\":2,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 2 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"sourceCase\":\"PROMO-EP-GLOBAL-BELOW-LIMIT\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-GLOBAL-BELOW-LIMIT\",\"kind\":\"usage\",\"name\":\"Áp mã khi tổng lượt chưa hết\",\"field\":\"usedCount so với maxUses\",\"rule\":\"Hợp lệ: usedCount < maxUses hoặc maxUses=null; Không hợp lệ: usedCount >= maxUses\",\"position\":\"Hợp lệ: usedCount < maxUses\",\"input\":\"usedCount=2; maxUses=3\",\"http\":200,\"expected\":\"Apply trả HTTP 200; preview không tăng usedCount.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"global\",\"used\":2,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 2 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"sourceCase\":\"PROMO-EP-GLOBAL-BELOW-LIMIT\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-GLOBAL-AT-LIMIT | Từ chối áp mã khi tổng lượt đã hết",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** usedCount so với maxUses\n\n**Các lớp:** Hợp lệ: usedCount < maxUses hoặc maxUses=null; Không hợp lệ: usedCount >= maxUses\n\n**Lớp được chọn:** Không hợp lệ: usedCount >= maxUses\n\n**Giá trị đại diện:** usedCount=3; maxUses=3\n\n**Kỳ vọng:** Apply trả HTTP 400; preview không tăng usedCount.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-GLOBAL-AT-LIMIT\",\"kind\":\"usage\",\"name\":\"Từ chối áp mã khi tổng lượt đã hết\",\"field\":\"usedCount so với maxUses\",\"rule\":\"Hợp lệ: usedCount < maxUses hoặc maxUses=null; Không hợp lệ: usedCount >= maxUses\",\"position\":\"Không hợp lệ: usedCount >= maxUses\",\"input\":\"usedCount=3; maxUses=3\",\"http\":400,\"expected\":\"Apply trả HTTP 400; preview không tăng usedCount.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"global\",\"used\":3,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 3 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"sourceCase\":\"PROMO-EP-GLOBAL-AT-LIMIT\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-GLOBAL-AT-LIMIT\",\"kind\":\"usage\",\"name\":\"Từ chối áp mã khi tổng lượt đã hết\",\"field\":\"usedCount so với maxUses\",\"rule\":\"Hợp lệ: usedCount < maxUses hoặc maxUses=null; Không hợp lệ: usedCount >= maxUses\",\"position\":\"Không hợp lệ: usedCount >= maxUses\",\"input\":\"usedCount=3; maxUses=3\",\"http\":400,\"expected\":\"Apply trả HTTP 400; preview không tăng usedCount.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"global\",\"used\":3,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 3 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"sourceCase\":\"PROMO-EP-GLOBAL-AT-LIMIT\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-USER-BELOW-LIMIT | Áp mã khi user chưa hết lượt",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** số lượt của user so với maxUsesPerUser\n\n**Các lớp:** Hợp lệ: userUsed < maxUsesPerUser hoặc giới hạn=null; Không hợp lệ: userUsed >= maxUsesPerUser\n\n**Lớp được chọn:** Hợp lệ: userUsed < maxUsesPerUser\n\n**Giá trị đại diện:** userUsed=2; maxUsesPerUser=3\n\n**Kỳ vọng:** Apply trả HTTP 200; preview không tạo thêm usage.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-USER-BELOW-LIMIT\",\"kind\":\"usage\",\"name\":\"Áp mã khi user chưa hết lượt\",\"field\":\"số lượt của user so với maxUsesPerUser\",\"rule\":\"Hợp lệ: userUsed < maxUsesPerUser hoặc giới hạn=null; Không hợp lệ: userUsed >= maxUsesPerUser\",\"position\":\"Hợp lệ: userUsed < maxUsesPerUser\",\"input\":\"userUsed=2; maxUsesPerUser=3\",\"http\":200,\"expected\":\"Apply trả HTTP 200; preview không tạo thêm usage.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"user\",\"used\":2,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 2 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"sourceCase\":\"PROMO-EP-USER-BELOW-LIMIT\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-USER-BELOW-LIMIT\",\"kind\":\"usage\",\"name\":\"Áp mã khi user chưa hết lượt\",\"field\":\"số lượt của user so với maxUsesPerUser\",\"rule\":\"Hợp lệ: userUsed < maxUsesPerUser hoặc giới hạn=null; Không hợp lệ: userUsed >= maxUsesPerUser\",\"position\":\"Hợp lệ: userUsed < maxUsesPerUser\",\"input\":\"userUsed=2; maxUsesPerUser=3\",\"http\":200,\"expected\":\"Apply trả HTTP 200; preview không tạo thêm usage.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"user\",\"used\":2,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 2 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"sourceCase\":\"PROMO-EP-USER-BELOW-LIMIT\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-USER-AT-LIMIT | Từ chối áp mã khi user đã hết lượt",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** số lượt của user so với maxUsesPerUser\n\n**Các lớp:** Hợp lệ: userUsed < maxUsesPerUser hoặc giới hạn=null; Không hợp lệ: userUsed >= maxUsesPerUser\n\n**Lớp được chọn:** Không hợp lệ: userUsed >= maxUsesPerUser\n\n**Giá trị đại diện:** userUsed=3; maxUsesPerUser=3\n\n**Kỳ vọng:** Apply trả HTTP 400; không tạo thêm usage.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-USER-AT-LIMIT\",\"kind\":\"usage\",\"name\":\"Từ chối áp mã khi user đã hết lượt\",\"field\":\"số lượt của user so với maxUsesPerUser\",\"rule\":\"Hợp lệ: userUsed < maxUsesPerUser hoặc giới hạn=null; Không hợp lệ: userUsed >= maxUsesPerUser\",\"position\":\"Không hợp lệ: userUsed >= maxUsesPerUser\",\"input\":\"userUsed=3; maxUsesPerUser=3\",\"http\":400,\"expected\":\"Apply trả HTTP 400; không tạo thêm usage.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"user\",\"used\":3,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 3 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"sourceCase\":\"PROMO-EP-USER-AT-LIMIT\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-USER-AT-LIMIT\",\"kind\":\"usage\",\"name\":\"Từ chối áp mã khi user đã hết lượt\",\"field\":\"số lượt của user so với maxUsesPerUser\",\"rule\":\"Hợp lệ: userUsed < maxUsesPerUser hoặc giới hạn=null; Không hợp lệ: userUsed >= maxUsesPerUser\",\"position\":\"Không hợp lệ: userUsed >= maxUsesPerUser\",\"input\":\"userUsed=3; maxUsesPerUser=3\",\"http\":400,\"expected\":\"Apply trả HTTP 400; không tạo thêm usage.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"user\",\"used\":3,\"method\":\"POST\",\"api\":\"/api/orders/apply-promo\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 3 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"sourceCase\":\"PROMO-EP-USER-AT-LIMIT\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-AVAILABLE-GLOBAL-EXHAUSTED | Ẩn mã đã hết tổng lượt khỏi danh sách available",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** usedCount so với maxUses\n\n**Các lớp:** Còn tổng lượt: xuất hiện; Hết tổng lượt: bị repository loại khỏi danh sách\n\n**Lớp được chọn:** Hết tổng lượt\n\n**Giá trị đại diện:** usedCount=3; maxUses=3\n\n**Kỳ vọng:** Available trả HTTP 200 và không chứa promotion fixture.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-AVAILABLE-GLOBAL-EXHAUSTED\",\"kind\":\"available\",\"name\":\"Ẩn mã đã hết tổng lượt khỏi danh sách available\",\"field\":\"usedCount so với maxUses\",\"rule\":\"Còn tổng lượt: xuất hiện; Hết tổng lượt: bị repository loại khỏi danh sách\",\"position\":\"Hết tổng lượt\",\"input\":\"usedCount=3; maxUses=3\",\"http\":200,\"expected\":\"Available trả HTTP 200 và không chứa promotion fixture.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"global\",\"used\":3,\"absent\":true,\"method\":\"POST\",\"api\":\"/api/promos/available\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 3 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-AVAILABLE-GLOBAL-EXHAUSTED\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-AVAILABLE-GLOBAL-EXHAUSTED\",\"kind\":\"available\",\"name\":\"Ẩn mã đã hết tổng lượt khỏi danh sách available\",\"field\":\"usedCount so với maxUses\",\"rule\":\"Còn tổng lượt: xuất hiện; Hết tổng lượt: bị repository loại khỏi danh sách\",\"position\":\"Hết tổng lượt\",\"input\":\"usedCount=3; maxUses=3\",\"http\":200,\"expected\":\"Available trả HTTP 200 và không chứa promotion fixture.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"global\",\"used\":3,\"absent\":true,\"method\":\"POST\",\"api\":\"/api/promos/available\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 3 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-AVAILABLE-GLOBAL-EXHAUSTED\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        },
        {
          "name": "PROMO-EP-AVAILABLE-USER-EXHAUSTED | Đánh dấu không đủ điều kiện khi user hết lượt",
          "request": {
            "method": "POST",
            "url": "{{request_url}}",
            "description": "**Task:** QLPT-352 / QLPT-347\n\n**Kỹ thuật:** Equivalence Partitioning\n\n**Input:** userUsed so với maxUsesPerUser\n\n**Các lớp:** User còn lượt: eligible; User hết lượt: vẫn có thể xuất hiện nhưng isEligible=false\n\n**Lớp được chọn:** User hết lượt\n\n**Giá trị đại diện:** userUsed=3; maxUsesPerUser=3\n\n**Kỳ vọng:** Available trả HTTP 200; promotion có isEligible=false và reason không rỗng.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{customer_token}}",
                  "type": "string"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{{request_body}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-AVAILABLE-USER-EXHAUSTED\",\"kind\":\"available\",\"name\":\"Đánh dấu không đủ điều kiện khi user hết lượt\",\"field\":\"userUsed so với maxUsesPerUser\",\"rule\":\"User còn lượt: eligible; User hết lượt: vẫn có thể xuất hiện nhưng isEligible=false\",\"position\":\"User hết lượt\",\"input\":\"userUsed=3; maxUsesPerUser=3\",\"http\":200,\"expected\":\"Available trả HTTP 200; promotion có isEligible=false và reason không rỗng.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"user\",\"used\":3,\"eligible\":false,\"method\":\"POST\",\"api\":\"/api/promos/available\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 3 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-AVAILABLE-USER-EXHAUSTED\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "try { await h.pre(); } catch (error) { await h.blocked(error); }"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const tc = {\"module\":\"Promotion\",\"id\":\"PROMO-EP-AVAILABLE-USER-EXHAUSTED\",\"kind\":\"available\",\"name\":\"Đánh dấu không đủ điều kiện khi user hết lượt\",\"field\":\"userUsed so với maxUsesPerUser\",\"rule\":\"User còn lượt: eligible; User hết lượt: vẫn có thể xuất hiện nhưng isEligible=false\",\"position\":\"User hết lượt\",\"input\":\"userUsed=3; maxUsesPerUser=3\",\"http\":200,\"expected\":\"Available trả HTTP 200; promotion có isEligible=false và reason không rỗng.\",\"technique\":\"Equivalence Partitioning\",\"basis\":\"SavePromotionRequest: value >= 0.01, minOrderValue >= 0, maxUses/maxUsesPerUser >= 1 hoặc null; AdminPromotionServiceImpl: PERCENT <= 100; OrderServiceImpl.validatePromo/calculateDiscount.\",\"issue\":\"QLPT-352\",\"limitType\":\"user\",\"used\":3,\"eligible\":false,\"method\":\"POST\",\"api\":\"/api/promos/available\",\"preconditions\":\"ADMIN + CUSTOMER test riêng; bật allow_test_writes. Script tạo mã EP_NHAN_ duy nhất, kiểm tra cấu hình, thực thi và vô hiệu hóa đúng mã vừa tạo. Bật allow_order_creation; variant đặt hàng riêng còn đủ kho; address thuộc user. Tạo 3 đơn COD/freeShipping để chuẩn bị lượt; đơn vẫn được giữ để làm bằng chứng.\",\"note\":\"Mỗi ca chọn một giá trị đại diện cho một lớp tương đương; kết quả phải giữ nguyên trạng thái dữ liệu khi request bị từ chối.\",\"sourceCase\":\"PROMO-EP-AVAILABLE-USER-EXHAUSTED\"};",
                  "const h = (function runtime(pm,tc) {",
                  " const env=k=>pm.environment.get(k);",
                  " const must=(ok,msg)=>{if(!ok)throw new Error(msg);};",
                  " const required=k=>{const v=env(k);must(v!==undefined&&v!==null&&String(v).trim()!==''&&!String(v).includes('{{'),'Thiếu environment: '+k);return v;};",
                  " const id=k=>{const n=Number(required(k));must(Number.isSafeInteger(n)&&n>0,'ID không hợp lệ: '+k);return n;};",
                  " const money=x=>Math.round(Number(x)*100);",
                  " const amount=x=>Number((x/100).toFixed(2));",
                  " let failed=false;",
                  " const check=(label,ok)=>{if(!ok)failed=true;pm.test('['+tc.id+'] '+label,()=>{pm.expect(Boolean(ok),label).to.equal(true);});};",
                  " const state=()=>JSON.parse(pm.variables.get('_ep_state')||'{}');",
                  " const save=s=>pm.variables.set('_ep_state',JSON.stringify(s));",
                  " function base(){const v=String(required('base_url')).replace(/\\/$/,'');must(/^https?:\\/\\/(localhost|127\\.0\\.0\\.1)(:\\d+)?$/i.test(v),'Chỉ chạy trên localhost/127.0.0.1, không có path/userinfo');return v;}",
                  " const headers=role=>({'Content-Type':'application/json',...(role?{Authorization:'Bearer '+required(role+'_token')}: {})});",
                  " async function request(method,path,body,role='customer'){",
                  "  must(path.startsWith('/api/'),'Path không nằm trong /api/');",
                  "  const config={method,url:base()+path,header:headers(role),followRedirects:false};",
                  "  if(body!==undefined)config.body={mode:'raw',raw:JSON.stringify(body)};",
                  "  const response=await pm.sendRequest(config);",
                  "  if([301,302,303,307,308].includes(response.code))throw new Error('Không chấp nhận redirect của API');",
                  "  return response;",
                  " }",
                  " async function json(method,path,body,role='customer',http=200){const r=await request(method,path,body,role);must(r.code===http,`Setup ${method} ${path}: HTTP ${r.code}, cần ${http}`);return r.json();}",
                  " async function fixture(key,stock,priceCheck=true){",
                  "  const prefix=key==='main'?'':key+'_';",
                  "  const productId=id(prefix+'product_id'),variantId=id(prefix+'variant_id');",
                  "  const slug=String(required(prefix+'product_slug'));",
                  "  const p=await json('GET','/api/products/'+encodeURIComponent(slug),undefined,null);",
                  "  must(Number(p.id)===productId,'Product ID không khớp slug');",
                  "  const v=(p.variants||[]).find(v=>Number(v.id)===variantId);must(v,'Không tìm thấy variant active');",
                  "  if(stock!==undefined)must(Number(v.stockQty)===stock,'Fixture tồn kho đã đổi, cần '+stock+' nhưng nhận '+v.stockQty);",
                  "  if(priceCheck){const priceKey=key==='price'?'price_unit_price':'unit_price';must(money(v.salePrice)===money(required(priceKey)),'Giá fixture khác '+priceKey);}",
                  "  return {productId,variantId,slug,p,v};",
                  " }",
                  " async function ownUser(){const u=await json('GET','/api/users/me');must(Number(u.id)===id('test_user_id')&&u.role==='CUSTOMER','Token không thuộc CUSTOMER test đã cấu hình');}",
                  " async function emptyCart(){const cart=await json('GET','/api/cart');must(Array.isArray(cart.items)&&cart.items.length===0,'Giỏ CUSTOMER test phải trống; script không xóa giỏ có sẵn');return cart;}",
                  " async function cartAdd(f,q){const r=await json('POST','/api/cart/items',{productId:f.productId,variantId:f.variantId,quantity:q});const item=r.items.find(i=>Number(i.variantId)===f.variantId);must(item&&Number(item.quantity)===q,'Không tạo được baseline giỏ');return item;}",
                  " function promoBody(){",
                  "  const now=Date.now(), fmt=t=>new Date(t).toISOString().slice(0,19);",
                  "  return {code:'EP_NHAN_'+Date.now().toString(36).toUpperCase()+'_'+Math.random().toString(36).slice(2,9).toUpperCase(),name:'EP_NHAN '+tc.id,type:'PERCENT',value:10,minOrderValue:0,freeShipping:true,target:'PUBLIC',userId:null,maxUses:null,maxUsesPerUser:null,startsAt:fmt(now-86400000),endsAt:fmt(now+86400000*2),isActive:true,...(tc.patch||{})};",
                  " }",
                  " async function counts(s){const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');const h=await json('GET','/api/admin/promotions/'+s.promoId+'/usages?page=0&size=100',undefined,'admin');must(h.last===true,'Lịch sử có nhiều trang; không kết luận thiếu dữ liệu');return {global:Number(p.usedCount),user:h.content.filter(x=>Number(x.userId)===id('test_user_id')).length};}",
                  " function discount(s){return s.promo.type==='PERCENT'?Math.floor(s.priceCents*Math.round(s.promo.value*100)/1000000)*100:Math.min(money(s.promo.value),s.priceCents);}",
                  " async function cleanup(){",
                  "  const s=state();",
                  "  // A cart is touched only after emptyCart succeeded; only this case's variant is removed.",
                  "  if(s.cartOwned&&s.cartVariant){try{const cart=await json('GET','/api/cart');for(const item of cart.items||[])if(Number(item.variantId)===s.cartVariant){const r=await request('DELETE','/api/cart/items/'+item.cartItemId);must(r.code===200,'Cleanup item thất bại');}}catch(e){check('Cleanup Cart',false);}}",
                  "  if(s.promoId&&s.promo){try{const p=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(p.code===s.promo.code&&p.name==='EP_NHAN '+tc.id,'Không vô hiệu hóa mã không do ca này tạo');if(p.isActive===true){const r=await request('PATCH','/api/admin/promotions/'+s.promoId+'/toggle-active',undefined,'admin');must(r.code===204,'Cleanup Promotion thất bại');}const after=await json('GET','/api/admin/promotions/'+s.promoId,undefined,'admin');must(after.isActive===false,'Mã vẫn active sau cleanup');}catch(e){check('Cleanup Promotion',false);}}",
                  " }",
                  " function record(status,http,result){const s=state();console.log('EP_RESULT '+JSON.stringify({tcId:tc.id,module:tc.module,issue:tc.issue,status,expectedHttp:tc.http,actualHttp:http,actualResult:result,promoId:s.promoId||null,promoCode:s.promo?.code||null,orderIds:s.orderIds||[],timestamp:new Date().toISOString()}));}",
                  " function brief(body){if(!body)return 'Không đọc được JSON';if(body.message)return String(body.message).slice(0,240);if(body.items)return JSON.stringify({id:body.id,totalAmount:body.totalAmount,items:body.items.map(i=>({variantId:i.variantId,quantity:i.quantity,subtotal:i.subtotal}))});if(body.code)return JSON.stringify({id:body.id,code:body.code,type:body.type,value:body.value,minOrderValue:body.minOrderValue,maxUses:body.maxUses,maxUsesPerUser:body.maxUsesPerUser});if(Array.isArray(body))return 'Nhận '+body.length+' mã; xem assertions';if(body.content)return 'Nhận '+body.content.length+' sản phẩm; xem assertions';return 'HTTP thành công; xem assertions';}",
                  " async function pre(){",
                  "  save({orderIds:[]}); base();",
                  "  pm.variables.set('request_url',base()+'/api/products');pm.variables.set('request_body','{}');",
                  "  const s=state(),priceKey=tc.kind==='productPrice'?'price_unit_price':'unit_price';s.priceCents=money(required(priceKey));must(Number.isSafeInteger(s.priceCents)&&s.priceCents>=100&&s.priceCents<=100000000,priceKey+' phải từ 1 đến 1,000,000 (giới hạn an toàn của bộ test, không phải yêu cầu nghiệp vụ)');save(s);",
                  "  if(tc.module!=='Product'){",
                  "   must(String(env('allow_test_writes'))==='true','Bật allow_test_writes=true sau khi duyệt dữ liệu test');",
                  "   must(String(env('dedicated_test_data_confirmed'))==='true','Chưa xác nhận dữ liệu/tài khoản dành riêng cho test');",
                  "   if(tc.used!==undefined)must(String(env('allow_order_creation'))==='true','Bật allow_order_creation=true; ca này tạo đơn COD test');",
                  "   await ownUser();",
                  "  }",
                  "  if(tc.module==='Product'){",
                  "   const f=await fixture(tc.kind==='productPrice'?'price':tc.key||'main',tc.stock,tc.kind==='productPrice'||tc.key==='main');s.f={productId:f.productId,variantId:f.variantId};save(s);",
                  "   if(tc.kind==='productStock')pm.variables.set('request_url',base()+'/api/products/'+encodeURIComponent(f.slug));",
                  "   else {must((f.p.variants||[]).every(v=>money(v.salePrice)===s.priceCents),'Fixture lọc giá phải có mọi variant active cùng giá S');const val=amount(s.priceCents+Math.round(tc.delta*100));pm.variables.set('request_url',base()+'/api/products?keyword='+encodeURIComponent(f.p.name)+'&page=0&size=100&sortBy=newest&'+tc.priceField+'='+val);}",
                  "   return;",
                  "  }",
                  "  if(tc.module==='Cart'){",
                  "   const f=await fixture(tc.key,tc.stock,tc.key==='main');await emptyCart();s.cartOwned=true;s.cartVariant=f.variantId;s.f={productId:f.productId,variantId:f.variantId};s.cartPrice=money(f.v.salePrice);save(s);",
                  "   if(tc.baseline){const item=await cartAdd(f,tc.baseline);s.itemId=item.cartItemId;save(s);}",
                  "   const body=tc.mode==='update'?{quantity:tc.q}:{productId:f.productId,variantId:f.variantId,quantity:tc.q};",
                  "   pm.variables.set('request_url',base()+'/api/cart/items'+(tc.mode==='update'?'/'+s.itemId:''));pm.variables.set('request_body',JSON.stringify(body));return;",
                  "  }",
                  "  required('admin_token');",
                  "  const admin=await json('GET','/api/users/me',undefined,'admin');must(admin.role==='ADMIN','Token admin không có role ADMIN');",
                  "  const f=await fixture('main',10,true);s.f={productId:f.productId,variantId:f.variantId};",
                  "  s.promo=promoBody();",
                  "  if(tc.valueDelta!==undefined)s.promo.value=amount(s.priceCents+Math.round(tc.valueDelta*100));",
                  "  if(tc.minDelta!==undefined)s.promo.minOrderValue=amount(s.priceCents+Math.round(tc.minDelta*100));",
                  "  save(s);",
                  "  if(tc.kind==='admin'){pm.variables.set('request_url',base()+'/api/admin/promotions');pm.variables.set('request_body',JSON.stringify(s.promo));return;}",
                  "  // Validate ALL order prerequisites before creating any promotion or order.",
                  "  if(tc.used!==undefined){",
                  "   const of=await fixture('order',undefined,false);must(of.variantId!==f.variantId,'Variant đặt đơn phải khác variant giỏ/giá');must(money(of.v.salePrice)>0,'Giá variant đặt đơn phải dương');must(Number(of.v.stockQty)>=tc.used+(tc.kind==='last'?1:0),'Thiếu kho cho ca đặt đơn');",
                  "   const a=await json('GET','/api/users/me/addresses/'+id('address_id'));must(Number(a.id)===id('address_id'),'Địa chỉ không thuộc tài khoản test');",
                  "   s.orderFixture={productId:of.productId,variantId:of.variantId,priceCents:money(of.v.salePrice)};",
                  "   if(tc.kind==='last'){await emptyCart();s.cartOwned=true;s.cartVariant=of.variantId;}",
                  "   save(s);",
                  "  }",
                  "  const created=await json('POST','/api/admin/promotions',s.promo,'admin',201);s.promoId=created.id;save(s);must(created.code===s.promo.code,'Code trả về không khớp');must(created.freeShipping===true,'Setup phải freeShipping để không gọi GHN');",
                  "  if(tc.used!==undefined){",
                  "   for(let i=0;i<tc.used;i++){",
                  "    const order=await json('POST','/api/orders/buy-now',{variantId:s.orderFixture.variantId,quantity:1,addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' setup'});",
                  "    s.orderIds.push(order.id);save(s);must(order.paymentMethod==='COD'&&Number(order.shippingFee)===0,'Setup không đúng COD/freeShipping');",
                  "   }",
                  "   s.promo[tc.limitType==='global'?'maxUses':'maxUsesPerUser']=3;",
                  "   const updated=await json('PUT','/api/admin/promotions/'+s.promoId,s.promo,'admin');save(s);",
                  "   must(Number(updated[tc.limitType==='global'?'maxUses':'maxUsesPerUser'])===3,'Không cập nhật đúng giới hạn');",
                  "   const c=await counts(s);must(c.global===tc.used&&c.user===tc.used,'Bộ đếm trước test không đúng dữ liệu chuẩn bị');s.beforeCounts=c;save(s);",
                  "  }else {s.beforeCounts=await counts(s);must(s.beforeCounts.global===0&&s.beforeCounts.user===0,'Mã mới phải có 0 lượt');save(s);}",
                  "  let path='/api/orders/apply-promo',body={variantId:f.variantId,quantity:1,promoCode:s.promo.code};",
                  "  if(tc.kind==='available'){path='/api/promos/available';body={variantId:f.variantId,quantity:1};}",
                  "  if(tc.kind==='last'){const item=await cartAdd(s.orderFixture,1);s.itemId=item.cartItemId;save(s);path='/api/orders';body={cartItemIds:[item.cartItemId],addressId:id('address_id'),paymentMethod:'COD',promoCode:s.promo.code,note:'EP_NHAN '+tc.id+' last-use'};}",
                  "  pm.variables.set('request_url',base()+path);pm.variables.set('request_body',JSON.stringify(body));",
                  " }",
                  " async function post(){",
                  "  let body;try{body=pm.response.json();}catch{check('Response là JSON',false);}",
                  "  check('HTTP '+tc.http,pm.response.code===tc.http);",
                  "  const s=state();",
                  "  try{",
                  "   if(tc.module==='Product'&&pm.response.code===200){",
                  "    if(tc.kind==='productStock'){check('Đúng product',Number(body.id)===s.f.productId);const v=body.variants.find(v=>Number(v.id)===s.f.variantId);check('Đúng tồn kho biên',v&&Number(v.stockQty)===tc.stock);}",
                  "    else {check('Bao phủ toàn bộ trang',body.last===true);check('Lọc giá đúng biên',body.content.some(p=>Number(p.id)===s.f.productId)===tc.present);}",
                  "   }",
                  "   if(tc.module==='Cart'){",
                  "    const cart=await json('GET','/api/cart');const own=cart.items.filter(i=>Number(i.variantId)===s.cartVariant);",
                  "    check('Không có item ngoài fixture',own.length===cart.items.length);",
                  "    check('Trạng thái giỏ đúng expected',tc.expectedQty===0?own.length===0:own.length===1&&Number(own[0].quantity)===tc.expectedQty);",
                  "    check('Tổng số lượng đúng',Number(cart.totalQuantity)===tc.expectedQty);",
                  "    check('Tổng tiền đúng',money(cart.totalAmount)===tc.expectedQty*s.cartPrice);",
                  "    if(own.length)check('Kho không bị trừ khi thêm giỏ',Number(own[0].stockQty)===tc.stock);",
                  "    if(pm.response.code===200){check('Response khớp giỏ được lưu',Number(body.totalQuantity)===Number(cart.totalQuantity)&&money(body.totalAmount)===money(cart.totalAmount));}",
                  "   }",
                  "   if(tc.module==='Promotion'){",
                  "    if(tc.kind==='admin'&&tc.http===400&&pm.response.code===400){const list=await json('GET','/api/admin/promotions?keyword='+encodeURIComponent(s.promo.code)+'&page=0&size=10',undefined,'admin');check('Không lưu mã bị từ chối',list.last===true&&!list.content.some(p=>p.code===s.promo.code));}",
                  "    if(tc.kind==='admin'&&pm.response.code===201){",
                  "     // Register this case's returned id before assertions, so a defect is still cleaned up safely.",
                  "     s.promoId=body.id;save(s);check('Đúng mã vừa tạo',body.code===s.promo.code);",
                  "     for(const key of ['type','value','minOrderValue','maxUses','maxUsesPerUser']){const hasOverride=tc.expectedStored&&Object.prototype.hasOwnProperty.call(tc.expectedStored,key);const expectedValue=hasOverride?tc.expectedStored[key]:s.promo[key];check('Lưu đúng '+key,body[key]===expectedValue);}",
                  "     check('Mã mới chưa dùng',Number(body.usedCount)===0);",
                  "     if(tc.http===201&&tc.apply){const r=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Customer apply HTTP 200',r.code===200);if(r.code===200){const b=r.json(),d=discount(s);check('Customer tính giảm đúng',money(b.subtotal)===s.priceCents&&money(b.discountAmount)===d&&money(b.totalAfterDiscount)===s.priceCents-d);}const c=await counts(s);check('Preview không tiêu hao lượt',c.global===0&&c.user===0);}",
                  "    }",
                  "    if(tc.kind==='preview'||tc.kind==='usage'){",
                  "     if(pm.response.code===200){const d=discount(s);check('Số tiền sau áp mã',body.promoCode===s.promo.code&&money(body.subtotal)===s.priceCents&&money(body.discountAmount)===d&&money(body.totalAfterDiscount)===s.priceCents-d);}",
                  "     if(tc.http===400)check('Lỗi nghiệp vụ rõ ràng',typeof body.message==='string'&&body.message.length>0);",
                  "     const c=await counts(s);check('Preview không thay bộ đếm',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);",
                  "    }",
                  "    if(tc.kind==='available'&&pm.response.code===200){const p=body.find(p=>p.code===s.promo.code);check('Danh sách và eligibility đúng',tc.absent?!p:!!p&&p.isEligible===tc.eligible);if(p&&tc.eligible===false)check('Có reason khi không đủ điều kiện',typeof p.reason==='string'&&p.reason.length>0);const c=await counts(s);check('Available không tăng lượt',c.global===s.beforeCounts.global&&c.user===s.beforeCounts.user);}",
                  "    if(tc.kind==='last'){",
                  "     if(pm.response.code===200){s.orderIds.push(body.id);save(s);const price=s.orderFixture.priceCents,d=Math.floor(price*1000/1000000)*100;check('Đơn COD và số tiền đúng',body.paymentMethod==='COD'&&body.promoCode===s.promo.code&&money(body.subtotal)===price&&money(body.discountAmount)===d&&money(body.totalAmount)===price-d&&Number(body.shippingFee)===0);}",
                  "     const c=await counts(s);check('Lượt cuối tăng đúng một lần',c.global===3&&c.user===3);const next=await request('POST','/api/orders/apply-promo',{variantId:s.f.variantId,quantity:1,promoCode:s.promo.code});check('Lần sau bị chặn HTTP 400',next.code===400);const cart=await json('GET','/api/cart');check('Item đã đặt rời giỏ',cart.items.length===0);",
                  "    }",
                  "   }",
                  "  }catch(e){check('Verification: '+e.message,false);}",
                  "  await cleanup();record(failed?'FAILED':'PASSED',pm.response.code,brief(body));",
                  " }",
                  " async function blocked(error){pm.test('['+tc.id+'] [BLOCKED] '+error.message,()=>{throw new Error(error.message);});await cleanup();record('BLOCKED',null,error.message);pm.execution.skipRequest();}",
                  " return {pre,post,blocked};",
                  "})(pm, tc);",
                  "await h.post();"
                ]
              }
            }
          ],
          "protocolProfileBehavior": {
            "followRedirects": false
          }
        }
      ]
    }
  ]
}
