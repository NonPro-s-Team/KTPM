# RMS Frontend

Ứng dụng quản lý phòng trọ được xây dựng bằng React, TypeScript và Vite.

## Khởi chạy

```bash
npm install
npm run dev
```

Sao chép `.env.example` thành `.env` khi cần kết nối API cục bộ.
