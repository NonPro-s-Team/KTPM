# QLPT-266 evidence

Results of the actual 2026-08-30 Maven run: 45 task tests passed. Full backend: 103 tests, 100 passed, 3 pre-existing skipped tests, no failures/errors. Screenshots capture a native read-only results window populated from Surefire XML and Maven output; they are not screenshots of Jira or an IDE test runner.

## Files to attach to Jira

- `01-test-summary.png`: task suite totals and BUILD SUCCESS.
- `02-service-cases.png`: 30 service unit test cases.
- `02-controller-cases.png`: 15 MockMvc integration cases.
- `maven-summary.txt`: actual Maven test summary lines.
- `JIRA-COMMENT.md`: suggested completion comment (not posted).
- `results.json`: case results extracted from Surefire XML.
- `summary.html` / `details.html`: readable generated reports.
- `com.*.txt`: original Surefire text reports for the complete backend run.

The full local `maven-verify.log` is ignored by Git and excluded from the upload ZIP because application logs may contain temporary test configuration. No credentials are required to view the evidence.

## Reproduce

From the project root in PowerShell:

```powershell
$env:JAVA_HOME = 'C:/Program Files/Java/jdk-21.0.12'
Push-Location backend
./mvnw.cmd -B '-Dmaven.repo.local=E:/KCPM/KTPM/.m2/repository' clean verify *> ../test-evidence/backend-automation/maven-verify.log
Pop-Location
./test-evidence/backend-automation/build-report.ps1
```

Capture script requires an interactive Windows desktop. Coverage is generated by JaCoCo in `backend/target/site/jacoco`. The test suites use mocks and a test MVC security configuration, not live payment/shipping or full database integration.

Existing disabled tests are in CartServiceImplTest, ProductServiceImplTest and PromotionServiceImplTest; no task test was disabled. Production code changes already present in the working tree were preserved. This work added OrderServiceImplTest and expanded the existing VNPay, OrderController and WebhookController tests.
